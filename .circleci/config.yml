version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:14.17.0
    working_directory: ~/server
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test