version: '3'
networks:
  demo-network:
    external: true
services:
  nginx:
    build: .
    ports:
      - "4001:4001"
    networks:
      - "demo-network"
    volumes:
      - ./nginx.conf:/config
  